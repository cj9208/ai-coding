"""quantdesk — the personal-quant research pipeline (M0 data plane).

Companion code to ``docs/personal-quant-bootstrap-plan.md``: Parquet is
the source of truth, downloads are checksum-verified against Binance's
public archive, and every layer is reachable through the ``quant`` CLI.
"""
